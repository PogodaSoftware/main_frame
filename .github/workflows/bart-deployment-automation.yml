name: Deploy to Main
on:
  pull_request:
    branches:
        - main
    types: [closed, synchronize]

defaults:
    run:
        working-directory: ../


jobs:
  deploy:
    # runs-on: ubuntu-latest test2
    runs-on: 'self-hosted'
    steps:
        - name: check working directory
          run: pwd

        - name: Checkout code
          uses: actions/checkout@v2
        - name: Check if Docker containers are running 
          id: docker-status 
          run: |
           if [ $(docker compose ps -q | wc -l) -gt 0 ]; 
           then 
             echo "Docker containers are running"
             sudo docker compose down
           else 
             echo "Docker containers are not running"  
           fi

        - name: Change directory into the directory
        # workdir: ./main_frame test
          working-directory: cd ./main_frame/main_frame
          run: pwd

        - name: List files in the directory test
          working-directory: ./main_frame/main_frame
          run: ls 

        - name: Branch Name
          working-directory: ./main_frame/main_frame
          run: git branch

        - name: Pull latest code
          working-directory: ./main_frame/main_frame
          run: git pull

        - name: Start Docker Compose
          run: docker-compose up -d