services:
  backend:
    environment:
      - BEAUTY_ADMIN_PRINCIPALS=customer:4
    ports:
      - "8000:8000"
  # App entry proxy moved off :4200 → :3001 (avoid the other app on :3000).
  frontend:
    ports: !override
      - "3001:80"
